spring:
    application:
        name: "DokOS REST API"
    data:
        rest:
            detection-strategy: "annotated"
    datasource:
        url: "jdbc:postgresql://dokos-db:5433/dokos-db"
        username: "SOkoD"
        password: "CheeseWIllAlwaysLose"
        driver-class-name: "org.postgresql.Driver"
    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: "org.hibernate.dialect.PostgreSQLDialect"
        hibernate:
            ddl-output: "update"
springdoc:
    swagger-ui:
        path: "/swagger-ui.html"
